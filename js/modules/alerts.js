export function initAlerts(){const form=document.querySelector("#alert-form");if(!form)return;form.addEventListener("submit",e=>{e.preventDefault();const name=form.elements.destination.value.trim(),date=form.elements.date.value;if(!name||!date){document.querySelector("#alert-result").textContent="Add a destination and target date.";return}const alert={destination:name,date,created:new Date().toISOString()};try{const old=JSON.parse(localStorage.getItem("irp-alerts")||"[]");old.push(alert);localStorage.setItem("irp-alerts",JSON.stringify(old));document.querySelector("#alert-result").textContent=`Reminder saved on this browser for ${name} on ${date}. This local reminder does not send SMS or run in the background. SMS delivery requires a configured backend and provider.`}catch{document.querySelector("#alert-result").textContent="Browser storage is unavailable. Please allow local storage or save this reminder elsewhere."}})}